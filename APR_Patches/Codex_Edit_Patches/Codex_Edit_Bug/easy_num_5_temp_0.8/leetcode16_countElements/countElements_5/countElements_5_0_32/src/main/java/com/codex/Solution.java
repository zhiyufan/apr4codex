package com.codex;

import java.util.*;

public class Solution {
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX_STR_SIZE 256
int main() {
    char str[MAX_STR_SIZE];
    gets(str);
    unsigned int alpha = 0, number = 0, space = 0, others = 0;
    for(int i = 0; i 
}