package com.codex;

import java.util.*;

public class Solution {
    public static List<Integer> targetIndices(int[] nums, int target, int k) {
        
        if (nums == null || nums.length == 0) return result;

        if (k <= 0) return result;

        for (int i = 0; i < nums.length; i++) {
            if (nums[i] == target) {
                result.add(i);
            }
        }

        for (int i = 0; i < k - 1; i++)
            result.remove(result.size() - 1);
        return result;
    }

  
            if (nums[i] == target) {
                result.add(i);
            }
        }

        for (int i = 0; i < k - 1; i++)
            result.remove(result.size() - 1);
        return result;
    }

  
            if (nums[i] == target) {
                result.add(i);
            }
        }

        for (int i = 0; i < k - 1; i++)
            result.remove(result.size() - 1);
        return result;
    }

  
            if (nums[i] == target) {
                result.add(i);
            }
        }

        for (int i = 0; i < k - 1; i++)
            result.remove(result.size() - 1);
        return result;
    }

  
            if (nums[i] == target) {
                result.add(i);
            }
        }

        for (int i = 0; i < k - 1; i++)
            result.remove(result.size() - 1);
        return result;
    }

  
            if (nums[i] == target) {
                result.add(i);
            }
        }

        for (int i = 0; i < k - 1; i++)
            result.remove(result.size() - 1);
        return result;
    }

  
            if (nums[i] == target) {
    }


        List<Integer> result = new ArrayList<>();
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] == target) {
                result.add(i);
            }
        }
        return result;
    }

    
}