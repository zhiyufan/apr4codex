package com.codex;

import java.util.*;

public class Solution {
    public static int minimumSum(int num) {
        
       
        
        int numStr = String.valueOf(num).length()-1;
        for (int i = 1; i < numStr; i++) {
            int num1 = Integer.valueOf(numStr.substring(0, i)).length();
            int num2 = Integer.valueOf(numStr.substring(i, numStr).length();
        }
        return minSum;
    }

    
}