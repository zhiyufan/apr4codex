package com.codex;

import java.util.*;

public class Solution {
         if ((Math.abs(start[0] - goal[0]) + Math.abs(start[1] - goal[1])) % 2 == 1) {
            return Collections.emptyList();
        }
}