package com.codex;

import java.util.*;

public class Solution {
       	if(head.data==x) {
	   		result=true;
	    		prev=temp;
			}
		else {
				if(head.data==y) {
						result=true;
						next=temp;
				}
			}
		temp=head;
		head=head.next;
		
		
    
    
}