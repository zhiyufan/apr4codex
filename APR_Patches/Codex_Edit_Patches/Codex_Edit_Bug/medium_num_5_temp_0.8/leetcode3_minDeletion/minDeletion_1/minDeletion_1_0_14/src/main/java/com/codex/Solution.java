package com.codex;

import java.util.*;

public class Solution {
int sum = 0;
    for (int i = 1; i < n; i++) {
        int tmp = i;
        while (tmp >= 1) {
            sum += tmp % 2;
            tmp /= 2;
        }
        return count;
    }

    
}