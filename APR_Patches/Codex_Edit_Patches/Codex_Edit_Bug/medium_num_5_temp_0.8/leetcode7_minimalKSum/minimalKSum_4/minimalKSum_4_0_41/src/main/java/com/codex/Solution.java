package com.codex;

import java.util.*;

public class Solution {
for (int j = 0; j < k; i++) {
            if (nums[j] > curr) {
                sum+=curr;
                curr++;
            }
        } 
}