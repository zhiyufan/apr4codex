package com.codex;

import java.util.*;

public class Solution {
    public void setId(String id) {
        this.id = id;
        this.idNum = Integer.parseInt(id.substring(1));
    }

        return collisions;
    }

    
}